import {registerHooks} from 'node:module';
import {readFile} from 'node:fs/promises';
import {fileURLToPath} from 'node:url';
import assert from 'node:assert/strict';
import * as T from '../dist/vendor/three.module.js';
import {phases} from '../dist/data.js';
registerHooks({resolve(s,c,next){if(s==='three')return{url:new URL('../dist/vendor/three.module.js',import.meta.url).href,shortCircuit:true};return next(s,c);}});
globalThis.ProgressEvent=class{constructor(t,v){Object.assign(this,v);}};globalThis.fetch=async r=>{const b=await readFile(fileURLToPath(typeof r==='string'?r:r.href||r.url));return new Response(b,{headers:{'Content-Length':String(b.length)}});};
const {loadOrganModels}=await import('../dist/organ-models.js');const assets=await loadOrganModels();for(const kind of ['heart','heartInternal','brain']){assert(assets[kind].meshes.length>0);const b=new T.Box3().setFromObject(assets[kind].group),s=b.getSize(new T.Vector3());assert(Math.abs(Math.max(s.x,s.y,s.z)-(kind==='brain'?1.24:.82))<1e-5);for(const m of assets[kind].meshes){assert(m.geometry.attributes.position.count>=3);assert(m.geometry.attributes.position.array.every(Number.isFinite));}console.log(kind,assets[kind].meshes.map(m=>m.geometry.index.count/3),'triangles');}
const dom=new Map(),$=s=>{if(!dom.has(s))dom.set(s,{checked:false,value:0,hidden:false,textContent:'',innerHTML:''});return dom.get(s);};globalThis.document={createElement:()=>({getContext:()=>({fillRect(){},fillText(){}})})};const {attachExplorer}=await import('../dist/explorer.js');const scene=new T.Scene(),fetus=new T.Group(),headGroup=new T.Group(),camera=new T.PerspectiveCamera(),solid=new T.MeshStandardMaterial(),grid=new T.Group();scene.add(fetus);fetus.add(headGroup);const body=new T.Mesh(new T.SphereGeometry(1),solid);fetus.add(body);const ell=(p,s,m,parent=fetus)=>{const o=new T.Mesh(new T.SphereGeometry(1,12,8),m);o.position.set(...p);o.scale.set(...s);parent.add(o);return o;};const api=attachExplorer({scene,camera,controls:{target:new T.Vector3(),update(){}},fetus,headGroup,ell,organMat:solid,boneMat:solid,grid,$,organAssets:assets});
api.select(phases[1].lessons.find(v=>v.id==='4ch'),1);assert(assets.heartInternal.group.visible);assert(!$('#planeToggle').disabled);assert(assets.heartInternal.meshes.every(m=>m.material.clippingPlanes.length===1));assert(scene.getObjectByName('heart-cut-tissue').geometry.attributes.position.count>0);api.source('reference');assert(assets.heart.group.visible);assert($('#planeToggle').disabled);api.source('schematic');assert(!assets.heart.group.visible);assert(!$('#planeToggle').disabled);assert.throws(()=>api.source('bad'));assert.equal(api.getState().sourceMode,'schematic');
const t=performance.now();api.select(phases[1].lessons[0],1);assert(api.getState().focus);assert(assets.brain.group.visible);assert(!assets.heart.group.visible);assert(body.material.transparent);assert.equal($('#focusLabel').textContent,'뇌 집중 보기');api.cut(true);assert.equal(assets.brain.meshes[0].material.clippingPlanes.length,1);api.angles({tilt:20,rock:10,rotation:15});api.offset(12);$('#organOnly').checked=true;api.isolate();assert(!body.visible);assert(assets.brain.group.visible);api.select(phases[1].lessons.find(v=>v.id==='ac'),1);assert(!assets.brain.group.visible);assert(body.visible);assert.equal(body.material,solid);console.log('Actual organ GLBs, reference/schematic switch, brain focus/cut/tilt/isolate and restoration passed.',Math.round(performance.now()-t),'ms for brain interactions. No GPU/browser visual validation.');

assert(assets.brain.volume.data.some(v=>v>0));console.log('Actual histology volume loaded.');
